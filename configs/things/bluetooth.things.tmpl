Bridge bluetooth:bluez:hci0 [
  address="{{ (ds "secrets").things.bluetooth.address }}",
  backgroundDiscovery=false
] {
  Thing beacon james_phone "James' Phone" [ address="{{ (ds "secrets").things.bluetooth.devices.james_phone.address }}" ]
}
