Bridge mqtt:broker:mosquitto [ host="mosquitto.data" ] {
  Thing mqtt:topic:james_mobile {
  Channels:
    Type number : rssi  "RSSI"  [ stateTopic="home/bluetooth/{{ (ds "secrets").things.bluetooth.james_mobile.mac_address }}/rssi" ]
    Type string : state "State" [ stateTopic="home/bluetooth/{{ (ds "secrets").things.bluetooth.james_mobile.mac_address }}/presence" ]
  }
}
