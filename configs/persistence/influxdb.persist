Strategies {
  everyMinute: "0 * * * * ?"
  everyHour:   "0 0 * * * ?"
  everyDay:    "0 0 0 * * ?"
}

Items {
  AirQuality*:   strategy = everyChange, restoreOnStartup
  Batteries*:    strategy = everyChange, restoreOnStartup
  Doors*:        strategy = everyChange, restoreOnStartup
  Temperatures*: strategy = everyChange, restoreOnStartup
  Network*:      strategy = everyChange, restoreOnStartup
  Presence*:     strategy = everyChange, restoreOnStartup
}
