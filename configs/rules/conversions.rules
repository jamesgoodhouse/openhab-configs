rule "Convert Front Door Switch to Contact"
when
  Item FrontDoorContactSensor_State changed
then
  FrontDoor_State.postUpdate(if(FrontDoorContactSensor_State.state == ON) OPEN else CLOSED)
end

rule "Convert Deck Door Switch to Contact"
when
  Item DeckDoorContactSensor_State changed
then
  DeckDoor_State.postUpdate(if(DeckDoorContactSensor_State.state == ON) OPEN else CLOSED)
end

rule "Convert Garage Entry Door Switch to Contact"
when
  Item GaragePersonDoorContactSensor_State changed
then
  GaragePersonDoor_State.postUpdate(if(GaragePersonDoorContactSensor_State.state == ON) OPEN else CLOSED)
end
