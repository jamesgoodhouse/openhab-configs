Group Network_SpeedTest "Speed Test" (Network) ["NetworkAppliance"]

String                  Speedtest_Running    "Test running ... [%s]"            (Network)                 { channel="network:speedtest:local:isRunning" }
Number:Dimensionless    Speedtest_Progress   "Test progress [%d %unit%]"        (Network)                 { channel="network:speedtest:local:progress" }
Number:DataTransferRate Speedtest_ResultDown "Downlink [%.2f %unit%]"           (Network) ["Measurement"] { channel="network:speedtest:local:rateDown" }
Number:DataTransferRate Speedtest_ResultUp   "Uplink [%.2f %unit%]"             (Network) ["Measurement"] { channel="network:speedtest:local:rateUp" }
DateTime                Speedtest_Start      "Test Start [%1$tH:%1$tM]"  <time> (Network)                 { channel="network:speedtest:local:testStart" }
DateTime                Speedtest_LUD        "Timestamp [%1$tH:%1$tM]"   <time> (Network)                 { channel="network:speedtest:local:testEnd" }

{{ range $n := (ds "secrets").things.networking -}}
{{ range $n.items -}}
{{ .type }} {{ $n.name }}_{{ .name }} (Home,IsHome_James,JamesPresence,Network) {{ "{" }} channel="network:{{ $n.type_id }}:{{ $n.thing_id }}:{{ .channel }}" {{ "}" }}
{{ end }}
{{ end -}}
